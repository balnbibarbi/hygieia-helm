apiVersion: v1
kind: Service
metadata:
  name: hygieia-headless
  labels:
    app: hygieia
spec:
  selector:
    app: hygieia
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  clusterIP: None
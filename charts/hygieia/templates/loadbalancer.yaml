apiVersion: v1
kind: Service
metadata:
  name: hygieia-0
spec:
  type: LoadBalancer
  selector:
    statefulset.kubernetes.io/pod-name: hygieia-0
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80